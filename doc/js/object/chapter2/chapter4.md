# 枚举属性
```
    除了检查对象的属性是否存在，遍历对象的属性，通常用for/in循环遍历 
    es5 提供了两个更好用的替代方案；for/in循环可以在循环体中遍历所有
    可枚举的属性(自有和继承)，对象继承的内置方法不可枚举，但代码中给
    对象添加的属性是可枚举的；

    ES5 枚举属性的函数：Object.keys(): 这个数组是可枚举的自有属性名称组成
    Object.getOwnPropertyNames() 自有属性名称，不只是可枚举的属性；

```

